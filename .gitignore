# Maven
/target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml
buildNumber.properties
.mvn/timing.properties
.mvn/wrapper/maven-wrapper.jar # Often included, but can be ignored if you prefer to manage it

# IDEs
.idea/                  # IntelliJ IDEA
.vscode/                # VS Code
*.iml                   # IntelliJ IDEA module files
.project                # Eclipse
.classpath              # Eclipse
.settings/              # Eclipse

# OS
.DS_Store               # macOS
Thumbs.db               # Windows

# Logs
*.log
logs/

# Environment variables / secrets (if not using Spring Cloud Config or similar)
# Be careful with application.properties if it contains sensitive data directly
# For Render, you'll use environment variables, so your application.properties
# should reference them like ${JWT_SECRET} and not contain the actual secret.
# If you temporarily put secrets in application.properties for local testing,
# you might want to uncomment this line, but ensure you don't commit actual secrets.
# application.properties